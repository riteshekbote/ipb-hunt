
===== TRIAGE 2026-09-14 23:40:32 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m→ [0mRead new-leads.txt
[0m→ [0mRead probe-results.md
[0m→ [0mRead probe-results.txt
[0m→ [0mRead scope.yml
[0m→ [0mRead leads
[0m→ [0mRead triage-output.txt
[0m→ [0mRead triage-prompt.txt
[0m→ [0mRead inventory
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/reposcan-ipb.md
[0m→ [0mRead inventory/ipb.md
No leads were appended after "LEADS:" in the triage prompt. I'll triage the distinct hypotheses extracted from the hunt bot lead files and probe results.

---

## LEAD 1: Cross-Tenant BOLA on EdgePortal Multi-Tenancy API
**Asset:** `pluto.portal.ipb.de /api/multi-tenancy/v1/tenant/{id}/, /user/{id}/, /association-request/{id}/`
**Confidence:** 60-65

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — company-owned customer portal, in scope per `scope.yml` |
| Q2 Reachable | **Partially** — endpoints are live (HTTP 401), but require auth (Token). Unauthenticated = blocked. |
| Q3 Impact | **HIGH** if confirmed — cross-tenant PII dump, privilege escalation |
| Q4 Provable | **No** — requires authenticated low-priv account. No self-registration path exists (all `_exceptions` routes SPA fallback; `user-registration/` 401; `tenant-registration/` 403). Cannot prove without a credentialed session. |
| Q5 Novel | Possibly novel, but unverifiable |
| Q6 Not always-rejected | Yes — IDOR/BOLA is in-scope class |
| Q7 Accept? | No — no PoC possible without credentials |

**Verdict: HOLD** — High-impact hypothesis, but no path to credentials. Blocked on HUMANAUTH. Cannot submit without proof.

---

## LEAD 2: Kiosk Token User-Enumeration Oracle
**Asset:** `pluto.portal.ipb.de /api/check-in/v1/check-in/kiosk_login/`
**Confidence:** 45

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — in-scope portal |
| Q2 Reachable | **Partially** — endpoint is live, returns distinct error `No User matches the given query.` for invalid tokens vs other errors |
| Q3 Impact | **LOW** — enumeration of valid kiosk tokens alone; weakens kiosk gate but no direct data exposure |
| Q4 Provable | **Maybe** — requires POST with 32-hex token candidates; response differentiation observed in SPA bundle but not live-validated from probe results. WAF/rate-limit risk. |
| Q5 Novel | Possibly, but low novelty |
| Q6 Not always-rejected | Yes — user enumeration via token oracle is not on always-rejected list (login enumeration is, but this is token-based) |
| Q7 Accept? | Unlikely — low severity, enumeration-only, no demonstrated exploit path |

**Verdict: HOLD** — Needs live confirmation of response differentiation. Low value alone. Would likely be closed as informational or low severity.

---

## LEAD 3: Admin Dynamic-Config Mass-Assignment
**Asset:** `pluto.portal.ipb.de /api/admin/dynamic-config/v1/config-namespace/apply/`
**Confidence:** 45

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — in-scope portal |
| Q2 Reachable | **No** — endpoint requires admin/elevated creds. No path to obtain admin creds. |
| Q3 Impact | **MEDIUM-HIGH** if confirmed — config tampering, DNS hijack via branding/motd/namespaces |
| Q4 Provable | **No** — needs admin session. Cannot prove without privileged account. |
| Q5 Novel | Possibly novel |
| Q6 Not always-rejected | Yes |
| Q7 Accept? | No — no PoC possible |

**Verdict: HOLD** — Same HUMANAUTH gate as Lead 1. Cannot prove without admin credentials.

---

## LEAD 4: EdgePortal Profile-Picture SVG Stored-XSS
**Asset:** `pluto.portal.ipb.de /api/multi-tenancy/v1/user/profile-picture/{upload,download}/`
**Confidence:** 45

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** |
| Q2 Reachable | **No** — both endpoints return 401 (auth-gated) |
| Q3 Impact | **HIGH** if confirmed — stored XSS via SVG content-type confusion |
| Q4 Provable | **No** — requires authenticated session to upload |
| Q5 Novel | Possibly novel |
| Q6 Not always-rejected | Yes — stored XSS is in-scope (self-XSS is rejected, but this is stored on another user's view) |
| Q7 Accept? | No — no proof |

**Verdict: HOLD** — Same auth gate. Cannot prove without credentials.

---

## LEAD 5: nc.ipb.de Nextcloud Status.php Version Leak
**Asset:** `nc.ipb.de/status.php`

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — company-owned Nextcloud |
| Q2 Reachable | **Yes** — confirmed live via `curl -k` |
| Q3 Impact | **None** — version disclosure is explicitly out-of-scope: "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" |
| Q4 Provable | Yes |
| Q5 Novel | No — Nextcloud version leak is well-known |
| Q6 Not always-rejected | **No** — falls under "descriptive error messages or headers / banner grabbing" out-of-scope |
| Q7 Accept? | No |

**Verdict: INVALID** — Always-rejected class (banner grabbing / version disclosure).

---

## LEAD 6: nc.ipb.de OCS Capabilities Unauthenticated Info Leak
**Asset:** `nc.ipb.de/ocs/v2.php/cloud/capabilities`

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** |
| Q2 Reachable | **Yes** — HTTP 200 with `OCS-APIRequest: true` header; leaks `app_api 34.0.0`, `bruteforce.delay=0`, theming config |
| Q3 Impact | **LOW** — reveals software version and configuration metadata; no PII or auth tokens. `provisioning_api`/`impersonate`/`oauth2`/`circles` NOT confirmed by live probe. |
| Q4 Provable | **Yes** — single GET with header, non-invasive |
| Q5 Novel | **No** — Nextcloud OCS capabilities endpoint is well-documented; information disclosure of this type is standard Nextcloud behavior |
| Q6 Not always-rejected | Borderline — "descriptive error messages or headers" may cover this; but OCS API metadata leak is slightly different |
| Q7 Accept? | **No** — standard Nextcloud behavior, no security impact, would be closed as informational |

**Verdict: INVALID** — Standard Nextcloud behavior, no real security impact, informational only.

---

## LEAD 7: Jitsi Meet config.js Public Exposure
**Asset:** `gold.ipb.de/config.js`

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — company-owned Jitsi instance |
| Q2 Reachable | **Yes** — HTTP 200 via `curl -k` |
| Q3 Impact | **None** — Jitsi config.js is designed to be publicly accessible (it configures the client-side app). Contains public server config, not secrets. `anonymousdomain` is by-design for guest access. |
| Q4 Provable | Yes |
| Q5 Novel | No — Jitsi config.js is always public |
| Q6 Not always-rejected | Borderline — "Disclosure of known public files or directories (e.g. robots.txt)" may apply |
| Q7 Accept? | No — by-design behavior |

**Verdict: INVALID** — By-design public file. No security impact.

---

## LEAD 8: Plesk Panel Forgery Protection Token (piwik/webcam.ipb.de)
**Asset:** `piwik.ipb.de/login.php`, `webcam.ipb.de/login.php`

| Gate | Answer |
|---|---|
| Q1 Scope | **Yes** — company-owned Plesk panels |
| Q2 Reachable | **Partially** — login page is accessible (303→/login.php), forgery_protection_token visible in meta tag |
| Q3 Impact | **HIGH** if static — CSRF on Plesk admin panel → admin takeover |
| Q4 Provable | **Partially** — need to confirm token is static across requests (two GETs, compare). Probe results show SSL cert fail in automated probes; manual `curl -k` needed. |
| Q5 Novel | **Possibly** — static CSRF token on Plesk is a known issue class, but specific to this instance |
| Q6 Not always-rejected | Borderline — "CSRF on forms available to anonymous users" is rejected, but this is login CSRF (pre-auth), which is higher value. "Clickjacking without additional details" is rejected but this isn't clickjacking. "Publicly accessible login panels" is rejected as a finding class, but CSRF on the login is a separate vulnerability. |
| Q7 Accept? | **Possibly** — if token is confirmed static, login CSRF on Plesk admin panel is a legitimate finding. But Plesk panels may be considered "publicly accessible login panels" which is explicitly out-of-scope. |

**Verdict: HOLD** — Needs staticity confirmation via two manual `curl -k` GETs. Borderline on "publicly accessible login panels" exclusion. If token is static, login CSRF is a valid pre-auth finding; if dynamic, INVALID. Cannot determine without live testing.

---

## LEAD 9: Reposcan — RackTables SHA-1 Password Hashing
**Asset:** `ipberlin/racktables/wwwroot/inc/auth.php:582`
**Confidence:** 95

| Gate | Answer |
|---|---|
| Q1 Scope | **Unclear** — this is from a `reposcan` of `ipberlin/racktables` repo. Is RackTables in-scope infrastructure? The `scope.yml` says "all company-owned infrastructure" and this is IPB's own RackTables instance code. |
| Q2 Reachable | **N/A** — code-level finding, not a live endpoint |
| Q3 Impact | **MEDIUM** — offline credential theft if DB compromised; but requires DB compromise first |
| Q4 Provable | **Yes** — code is in the repo, SHA-1 usage is confirmed by grep |
| Q5 Novel | **No** — SHA-1 hashing in RackTables is a known upstream issue (CVE-2019-XXXXX class). RackTables is legacy software. |
| Q6 Not always-rejected | "Use of a known-vulnerable library without description of an exploit specific to our implementation" — this is a known-vulnerable pattern. |
| Q7 Accept? | **No** — known upstream issue, no exploit specific to IPB's implementation |

**Verdict: INVALID** — Known-vulnerable library pattern, explicitly out-of-scope per program rules.

---

## LEAD 10: Reposcan — RackTables SSH StrictHostKeyChecking Disabled
**Asset:** `ipberlin/racktables/wwwroot/inc/remote.php:328`
**Confidence:** 90

| Gate | Answer |
|---|---|
| Q1 Scope | **Unclear** — same question as Lead 9 about RackTables scope |
| Q2 Reachable | **N/A** — code-level finding |
| Q3 Impact | **HIGH** if live — MITM on network device management, credential theft |
| Q4 Provable | **Yes** — code confirms `StrictHostKeyChecking no` |
| Q5 Novel | **Possibly novel** for this specific deployment |
| Q6 Not always-rejected | Yes — SSH misconfig is not on the always-rejected list |
| Q7 Accept? | **Possibly** — but requires confirmation that this code is actually deployed and used. "Attacks requiring physical access" and "social engineering" are rejected, but network-level MITM is different. However, this is a code review finding, not a live test. If the RackTables instance is in scope and reachable, this is reportable. |

**Verdict: HOLD** — Needs confirmation: (1) is RackTables actually deployed/accessible? (2) is `remote.php` actively used for device management? Cannot determine from code review alone. If deployed + reachable, this is a valid finding.

---

## LEAD 11: Reposcan — RackTables Reflected XSS
**Asset:** `ipberlin/racktables/wwwroot/inc/ajax-interface.php:122,148`
**Confidence:** 80

| Gate | Answer |
|---|---|
| Q1 Scope | **Unclear** — RackTables scope question |
| Q2 Reachable | **Unknown** — needs live RackTables instance confirmation |
| Q3 Impact | **MEDIUM** — session hijacking via XSS |
| Q4 Provable | **Partially** — code shows `$_REQUEST['tagid']` reflected in error messages, but no live testing performed |
| Q5 Novel | **Possibly** — but reflected XSS in RackTables may be known upstream |
| Q6 Not always-rejected | Yes |
| Q7 Accept? | **Possibly** — but needs live confirmation. Also "Self-XSS and issues exploitable only through Self-XSS" is rejected; reflected XSS in error messages could be self-XSS if only triggered by the victim's own input |

**Verdict: HOLD** — Needs live RackTables instance confirmation and demonstration that XSS is triggered by another user's input (not self-XSS).

---

## LEAD 12: Reposcan — RackTables Insecure secret.php Permissions
**Asset:** `ipberlin/racktables/wwwroot/inc/install.php:148`
**Confidence:** 85

| Gate | Answer |
|---|---|
| Q1 Scope | **Unclear** — RackTables scope question |
| Q2 Reachable | **N/A** — code-level recommendation, not a live endpoint |
| Q3 Impact | **MEDIUM** — local privilege escalation if `chmod 666` is used |
| Q4 Provable | **Yes** — code shows the instruction |
| Q5 Novel | **No** — known RackTables installation issue |
| Q6 Not always-rejected | "Known-vulnerable library" may apply |
| Q7 Accept? | **No** — installation instruction, not a live vulnerability; would need `secret.php` to actually have 666 perms |

**Verdict: INVALID** — Installation recommendation, not a confirmed live vulnerability. No proof of actual insecure permissions.

---

## LEAD 13: Reposcan — RackTables Debug Exception Info Leak
**Asset:** `ipberlin/racktables/wwwroot/inc/exceptions.php:268-272,293-297`
**Confidence:** 70

| Gate | Answer |
|---|---|
| Q1 Scope | **Unclear** |
| Q2 Reachable | **Unknown** — needs live instance |
| Q3 Impact | **LOW** — information disclosure on unhandled exceptions |
| Q4 Provable | **Partially** — code review shows request params dumped |
| Q5 Novel | **No** — standard debug exception behavior |
| Q6 Not always-rejected | "Descriptive error messages or headers (e.g. Stack Traces)" is explicitly out-of-scope |
| Q7 Accept? | **No** |

**Verdict: INVALID** — Always-rejected class (descriptive error messages / stack traces).

---

## Summary

| # | Lead | Verdict | Reason |
|---|---|---|---|
| 1 | Cross-Tenant BOLA (pluto.portal) | **HOLD** | High impact but auth-gated; no credential path; needs HUMANAUTH |
| 2 | Kiosk Token Enumeration | **HOLD** | Low impact, needs live response confirmation |
| 3 | Admin Dynamic-Config Mass-Assignment | **HOLD** | Admin-cred-gated; no credential path |
| 4 | Profile-Picture SVG Stored-XSS | **HOLD** | Auth-gated; no credential path |
| 5 | Nextcloud status.php Version Leak | **INVALID** | Out-of-scope (banner grabbing) |
| 6 | Nextcloud OCS Capabilities Leak | **INVALID** | Standard behavior, no real impact |
| 7 | Jitsi config.js Public Exposure | **INVALID** | By-design public file |
| 8 | Plesk Forgery Token CSRF | **HOLD** | Needs staticity confirmation; borderline on "public login panels" |
| 9 | RackTables SHA-1 Hashing | **INVALID** | Known-vulnerable library pattern, out-of-scope |
| 10 | RackTables SSH StrictHostKeyChecking | **HOLD** | Code review only; needs live deployment confirmation |
| 11 | RackTables Reflected XSS | **HOLD** | Needs live instance + self-XSS exclusion check |
| 12 | RackTables secret.php Permissions | **INVALID** | Installation recommendation, not live vuln |
| 13 | RackTables Debug Info Leak | **INVALID** | Out-of-scope (descriptive error messages) |

**Zero leads ready for immediate submission.** The highest-value lead (#1 Cross-Tenant BOLA) is blocked entirely on credential acquisition. Leads 9-13 from reposcan need scope confirmation for whether RackTables is in-scope infrastructure.
