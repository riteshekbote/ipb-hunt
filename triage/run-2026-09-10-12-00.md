
===== TRIAGE 2026-09-10 12:00:00 UTC =====

> model · mimo-v2.5-free
> scope · scope.yml / inventory/ipb.md
> probe · passive per probe-results.md + curl -k manual (nc, gold)

LEAD 1: Cross-tenant BOLA on EdgePortal multi-tenancy API
─────────────────────────────────────────────────────────
class: IDOR
asset: pluto.portal.ipb.de /api/multi-tenancy/v1/{tenant,user,association-request,user-token,membership}/
confidence: 65
source: bigpickle (repeated 10+ cycles), nemotron3 (repeated 8+ cycles)

Q1 In scope?
YES — pluto.portal.ipb.de is IPB-owned infrastructure; program scope: "All company-owned infrastructure including data centers, networks, cloud platforms, hosting systems, APIs, customer portals, and domains operated by IPB."

Q2 Attacker reachable?
YES — all endpoints return HTTP 401 (WWW-Authenticate: Token) from public internet; no VPN/allowlist. An attacker who obtains ANY valid tenant token can reach every object endpoint.

Q3 Real security impact?
YES — cross-tenant PII dump (user records, membership, association-request data, check-in data) across sequential IDs = CRITICAL. DRF multi-tenancy means a low-priv tenant account accesses all tenant data by ID iteration.

Q4 Provable non-invasively?
PARTIAL — endpoints confirmed live via passive GET probes (401 unauthenticated); OPTIONS exposes seq-ID serializer schemas; per-tenant Token auth is sole cross-tenant control. Full proof requires two tenant accounts (HUMAN_ONLY).

Q5 Novel/unreported?
YES — no prior report on bugs.olivermaicher.eu found; sequential-ID BOLA on DRF multi-tenancy is a known pattern but IPB-specific instance is novel.

Q6 Not always-rejected?
YES — IDOR/BOLA is a core accepted class; not on always-rejected list.

Q7 Reasonable triager accept?
HOLD — hypothesis is high-confidence and high-impact but is HUMAN_ONLY (requires two credentialed tenant accounts to prove). Cannot file as VALID without exploit PoC. Cannot file as INVALID (pattern is real). Recommend: HOLD pending credential acquisition.

VERDICT: HOLD
reason: High-confidence CRITICAL BOLA pattern on live DRF multi-tenancy API; gated on obtaining two tenant accounts for PoC; no unauthenticated proof path exists.
CVSS 3.1: (if confirmed) 8.8 — AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N
reporting channel: bugs.olivermaicher.eu (Oliver Maicher private program)


LEAD 2: EdgePortal profile-picture upload stored-XSS via content-type confusion
───────────────────────────────────────────────────────────────────────────────
class: XSS
asset: pluto.portal.ipb.de /api/multi-tenancy/v1/user/profile-picture/{upload,download}/
confidence: 45
source: bigpickle (since 2026-09-05)

Q1 In scope?
YES — pluto.portal.ipb.de, IPB-owned multi-tenant customer portal.

Q2 Attacker reachable?
YES — POST upload/ returns 401 (auth-gated from public internet); download/ also 401. Any valid tenant token can upload and download.

Q3 Real security impact?
YES — stored XSS against admin/staff viewing tenant profile pictures = session theft → full multi-tenant compromise. HIGH impact chain.

Q4 Provable non-invasively?
PARTIAL — upload endpoint confirmed auth-gated; avatar served via /download/ on portal SPA origin. Full proof requires uploading SVG/HTML payload with auth (HUMAN_ONLY).

Q5 Novel/unreported?
YES — no prior report found; content-type confusion on multi-tenant profile picture upload is a specific novel attack path.

Q6 Not always-rejected?
YES — stored XSS is a core accepted class.

Q7 Reasonable triager accept?
HOLD — hypothesis is plausible (auth-gated upload → inline serve on SPA origin) but requires credential + payload upload to prove. Cannot validate without PoC.

VERDICT: HOLD
reason: Plausible stored-XSS chain via profile-picture SVG upload; auth-gated, HUMAN_ONLY; needs credential + upload PoC to validate.
CVSS 3.1: (if confirmed) 8.1 — AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N
reporting channel: bugs.olivermaicher.eu


LEAD 3: Nextcloud AppAPI ExApp registry mis-scoping
─────────────────────────────────────────────────────
class: AUTH
asset: nc.ipb.de /ocs/v2.php/apps/app_api/apps/list (+ ExApp registered routes)
confidence: 40
source: bigpickle (since 2026-09-05, corrected from earlier overstatement)

Q1 In scope?
YES — nc.ipb.de is IPB-owned Nextcloud 34.0.3 instance behind wildcard proxy; confirmed live via curl -k.

Q2 Attacker reachable?
YES — nc.ipb.de is publicly reachable (SSL cert mismatch via wildcard proxy, curl -k works); OCS capabilities endpoint confirmed unauthenticated 200.

Q3 Real security impact?
YES — if AppAPI ExApps are registered, mis-scoped external app runtime could expose auth bypass, SSRF, or data exfiltration. HIGH impact.

Q4 Provable non-invasively?
NO — unauth GET /ocs/v2.php/apps/app_api/apps/list returns 404 (routing exists but empty/no-session); /cloud/apps returns 401. Requires valid NC session to enumerate.

Q5 Novel/unreported?
YES — NC 34.0.3 AppAPI (ExApps) is a relatively new feature; mis-scoping on hosted instances is under-researched.

Q6 Not always-rejected?
YES — auth mis-scoping is accepted; not on always-rejected list.

Q7 Reasonable triager accept?
HOLD — pattern is real (app_api 34.0.0 confirmed live via capabilities) but unauthenticated path returns 404/401; needs session to enumerate ExApps. Cannot prove without credentials.

VERDICT: HOLD
reason: Live Nextcloud 34.0.3 with app_api 34.0.0 confirmed; ExApp registry requires session to enumerate; HUMAN_ONLY for PoC.
CVSS 3.1: (if confirmed) 8.8 — AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N
reporting channel: bugs.olivermaicher.eu


LEAD 4: Plesk Panel forgery_protection_token static CSRF
─────────────────────────────────────────────────────────
class: AUTH
asset: piwik.ipb.de, webcam.ipb.de (Plesk Panel 18.0.80-6)
confidence: 45
source: bigpickle (since 2026-09-04)

Q1 In scope?
YES — piwik.ipb.de and webcam.ipb.de are IPB-owned Plesk Panel instances.

Q2 Attacker reachable?
YES — Plesk login pages are publicly accessible (303→/login.php); forgery_protection_token visible in HTML meta tag.

Q3 Real security impact?
CONDITIONAL — if token is static per session, CSRF on Plesk admin login = admin takeover = HIGH. If token rotates per request, impact drops to near-zero.

Q4 Provable non-invasively?
PARTIAL — token visible in HTML meta; two GET requests to /login.php would confirm staticity (passive). But: program scope explicitly lists "CSRF on forms that are available to anonymous users" as ALWAYS REJECTED. Plesk login is a public form → out-of-scope class.

Q5 Novel/unreported?
NO — Plesk Panel CSRF on login is a well-known class; not novel.

Q6 Not always-rejected?
NO — scope.yml explicitly rejects: "CSRF on forms that are available to anonymous users" and "Publicly accessible login panels." Plesk login page is both. ALWAYS REJECTED.

Q7 Reasonable triager accept?
NO — explicitly out-of-scope per program rules. Will be rejected.

VERDICT: INVALID
reason: Always-rejected class: "CSRF on forms available to anonymous users" + "Publicly accessible login panels." Plesk login page qualifies for both exclusions.
CVSS 3.1: N/A (out-of-scope class)
reporting channel: N/A


LEAD 5: Check-in kiosk token user-enumeration oracle
─────────────────────────────────────────────────────
class: AUTH
asset: pluto.portal.ipb.de /api/check-in/v1/check-in/kiosk_login/
confidence: 45
source: bigpickle, nemotron3 (since 2026-09-03)

Q1 In scope?
YES — pluto.portal.ipb.de, IPB-owned.

Q2 Attacker reachable?
YES — kiosk_login endpoint returns distinct responses for valid vs invalid tokens unauthenticated.

Q3 Real security impact?
LOW — token existence oracle alone is low severity; weakens kiosk gate for targeted brute-force.

Q4 Provable non-invasively?
PARTIAL — response differentiation confirmed in bundle; but token format is 32-hex; enumeration at scale likely triggers WAF/rate-limit.

Q5 Novel/unreported?
MARGINAL — token oracle via distinct error messages is a known pattern.

Q6 Not always-rejected?
CONDITIONAL — scope.yml rejects "Username enumeration based on login or forgot password pages" and "Enforcement policies for brute force, rate limiting, or account lockout." Kiosk token enumeration is not exactly username enumeration, but the endpoint is a public login panel. BORDERLINE.

Q7 Reasonable triager accept?
NO — low impact + borderline on always-rejected "public login panels" + "username enumeration" classes. Likely rejected.

VERDICT: INVALID
reason: Low impact alone; borderline on always-rejected "username enumeration" and "publicly accessible login panels" classes; no realistic path to exploit.
CVSS 3.1: N/A (low impact, borderline OOS)
reporting channel: N/A


LEAD 6: Admin dynamic-config mass-assignment override
─────────────────────────────────────────────────────
class: MISCONFIG
asset: pluto.portal.ipb.de /api/admin/dynamic-config/v1/config-namespace/apply/
confidence: 45
source: bigpickle, nemotron3 (since 2026-09-03)

Q1 In scope?
YES — pluto.portal.ipb.de, IPB-owned.

Q2 Attacker reachable?
PARTIAL — endpoint returns 401 (auth-gated); SPA bundle calls adminCreateDynamicConfig with arbitrary keys. Admin-level session required.

Q3 Real security impact?
YES — config tampering / defacement / DNS hijack via branding/motd/dns namespaces = MEDIUM-HIGH.

Q4 Provable non-invasively?
NO — requires admin-level session to test cross-namespace payload write. HUMAN_ONLY.

Q5 Novel/unreported?
MARGINAL — DRF mass-assignment is a known class; IPB-specific instance is not yet proven.

Q6 Not always-rejected?
YES — misconfiguration on authenticated endpoints is accepted.

Q7 Reasonable triager accept?
HOLD — plausible but entirely HUMAN_ONLY; no unauthenticated proof path; confidence below 50.

VERDICT: HOLD
reason: Plausible admin-level mass-assignment; entirely credential-gated; confidence below 50; cannot advance without admin creds.
CVSS 3.1: (if confirmed) 7.2 — AV:N/AC:L/PR:H/UI:N/S:U/C:N/I:H/A:N
reporting channel: bugs.olivermaicher.eu


LEAD 7: Wildcard DNS masking hidden attack surface
───────────────────────────────────────────────────
class: MISCONFIG
asset: *.ipb.de (multiple subdomains)
confidence: 60
source: bigpickle, nemotron3 (repeated across many cycles)

Q1 In scope?
YES — *.ipb.de wildcard DNS on IPB-owned infrastructure.

Q2 Attacker reachable?
YES — wildcard DNS resolves all *.ipb.de to shared IP; but app/auth.gold/cloud/my/prod have DNS failures or SSL cert mismatches.

Q3 Real security impact?
CONDITIONAL — IF hidden services exist behind wildcard, full attack surface is hidden. But: 33/33 hosts fully classified (Plesk×3, DNS-dead×15, non-web×6, Jitsi×3, live×7); zero genuinely dedicated hosts confirmed. Impact depends on discovery of unclassified host.

Q4 Provable non-invasively?
PARTIAL — wildcard DNS confirmed; but no distinct content found from any subdomain not already classified. All Host-header probes would be active testing.

Q5 Novel/unreported?
NO — wildcard DNS on shared hosting is a well-known architectural pattern; not novel.

Q6 Not always-rejected?
YES — misconfiguration is accepted.

Q7 Reasonable triager accept?
NO — hypothesis has been fully investigated over 15+ cycles; 33/33 hosts classified; zero new live hosts discovered; surface fully converged. No remaining unclassified subdomain to prove hidden service existence.

VERDICT: INVALID
reason: Fully saturated over 15+ probe cycles; 33/33 inventory hosts classified; zero new live hosts discovered; no remaining evidence of hidden services behind wildcard.
CVSS 3.1: N/A (no proven hidden service)
reporting channel: N/A


══════════════════════════════════════
SUMMARY
══════════════════════════════════════

| # | Lead | Verdict | Action |
|---|------|---------|--------|
| 1 | Cross-tenant BOLA (EdgePortal) | HOLD | Needs two tenant accounts for PoC |
| 2 | Profile-picture stored-XSS | HOLD | Needs credential + SVG upload PoC |
| 3 | NC AppAPI ExApp mis-scoping | HOLD | Needs NC session to enumerate ExApps |
| 4 | Plesk forgery_protection_token CSRF | INVALID | Always-rejected: public login panel CSRF |
| 5 | Kiosk token user-enumeration | INVALID | Low impact + borderline OOS |
| 6 | Admin dynamic-config mass-assignment | HOLD | Needs admin creds; confidence <50 |
| 7 | Wildcard DNS hidden surface | INVALID | Fully saturated; 33/33 classified |

VALID bugs: 0
HOLD (needs credential): 3
INVALID (rejected): 4

RECOMMENDED NEXT ACTIONS:
- Credential acquisition for pluto.portal.ipb.de (removes HUMAN gate on leads 1, 2, 6)
- Credential acquisition for nc.ipb.de (removes HUMAN gate on lead 3)
- All remaining high-value leads (BOLA 65, XSS 45, AppAPI 40) are gated on human account sponsorship
